[package]
name = "ferrite-lancedb-spike"
version = "0.1.0"
edition = "2024"
publish = false

# Keep this throwaway driver out of Ferrite DB's root workspace.
[workspace]

[dependencies]
futures = "0.3"
lancedb = "0.37.1"
tempfile = "3.23"
tokio = { version = "1.48", features = ["macros", "rt-multi-thread"] }
