//! Search — exhaustive scanning, Predicate Tree evaluation, Probe budgeting,
//! options plumbing, result assembly (AGENTS.md §4). Queries are always
//! scoped to exactly one Table (ADR 0005). Owned by ROADMAP FDB-014;
//! intentionally empty until then.
