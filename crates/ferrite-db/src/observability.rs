//! Observability — optional `tracing` feature: spans and histograms behind
//! gated macros; default build carries zero observability cost (AGENTS.md
//! §4). Owned by ROADMAP FDB-060; intentionally empty until then.
