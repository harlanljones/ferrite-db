#[test]
fn scaffold_compiles_and_links() {
    // Placeholder proving the crate builds and links into a test binary
    // before any owning work item lands (FDB-002).
    assert!(std::any::TypeId::of::<()>() == std::any::TypeId::of::<()>());
}
